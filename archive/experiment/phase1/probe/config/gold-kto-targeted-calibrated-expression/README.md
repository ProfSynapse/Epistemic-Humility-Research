# Gold KTO Targeted Calibrated-Expression Config Archive

This directory archives the legacy Phase 3 targeted gold-backed KTO calibrated-expression axis-scan and direction-export configs formerly stored under `experiment/phase1/probe/config/`.

Migration batch: `C014` from `docs/migration/phase1-probe-config-terrain.md`.

Owner decision: archive-only historical provenance for the June 20 Phase 3 behavior-conditioned SAE / targeted gold-backed KTO calibrated-expression follow-up. No migrated `experiments/<slug>` owner was present, and these configs were not reusable shared defaults at migration time.

The component contains the enriched targeted-panel behavior-axis scan and direction export used before the targeted simple-axis causal follow-up. Keep the configs as provenance for historical screening. New evidence-producing cells belong under `experiments/<slug>/` or `experiments/common/` when promoted for shared reuse.

Non-goal: legacy output roots under `experiment/phase1/probe/qwen3-4b-instruct/` are preserved as historical run provenance and will be handled in broader artifact archive passes.
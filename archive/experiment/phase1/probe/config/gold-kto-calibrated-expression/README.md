# Gold KTO Calibrated-Expression Config Archive

This directory archives the legacy Phase 3 gold-backed KTO calibrated-expression axis-scan and direction-export configs formerly stored under `experiment/phase1/probe/config/`.

Migration batch: `C013` from `docs/migration/phase1-probe-config-terrain.md`.

Owner decision: archive-only historical provenance for the June 20 Phase 3 behavior-conditioned SAE / gold-backed KTO calibrated-expression screening session. No migrated `experiments/<slug>` owner was present, and these configs were not reusable shared defaults at migration time.

The component contains the base gold-backed KTO behavior-axis scan, the first direction export, and the same-layer direction export. Keep the configs as provenance for historical screening. New evidence-producing cells belong under `experiments/<slug>/` or `experiments/common/` when promoted for shared reuse.

Non-goal: legacy output roots under `experiment/phase1/probe/qwen3-4b-instruct/` are preserved as historical run provenance and will be handled in broader artifact archive passes.
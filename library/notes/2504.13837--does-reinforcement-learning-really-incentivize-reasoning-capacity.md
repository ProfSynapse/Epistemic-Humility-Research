---
title: 'Does Reinforcement Learning Really Incentivize Reasoning Capacity in LLMs Beyond the Base Model?'
arxiv: '2504.13837'
year: 2025
url: https://arxiv.org/abs/2504.13837
area: verification
status: verified
tags:
- kg/paper
- paper
- epistemic-humility
- verification
authors:
- Yang Yue
- Zhiqi Chen
- Rui Lu
- Andrew Zhao
- Zhaokai Wang
- Yang Yue
- Shiji Song
- Gao Huang
models: []
metrics: []
pdf: ../pdfs/2504.13837.pdf
kg:
  id: paper:2504.13837
  type: paper
  status: canonical
related:
- '[[rlvr-reasoning-bounded-by-base-model]]'
- '[[distillation-expands-reasoning-boundary-beyond-base]]'
- '[[pass-at-k]]'
- '[[policy-entropy-collapse-narrows-rlvr-reasoning-paths]]'
- '[[only-sft-installs-abstention-in-weights]]'
relationships:
- type: measures
  target: '[[pass-at-k]]'
  target_id: metric:pass-at-k
  confidence: high
- type: supports
  target: '[[rlvr-reasoning-bounded-by-base-model]]'
  target_id: mechanism:rlvr-reasoning-bounded-by-base-model
  confidence: high
- type: supports
  target: '[[distillation-expands-reasoning-boundary-beyond-base]]'
  target_id: mechanism:distillation-expands-reasoning-boundary-beyond-base
  confidence: high
- type: related_to
  target: '[[policy-entropy-collapse-narrows-rlvr-reasoning-paths]]'
  target_id: mechanism:policy-entropy-collapse-narrows-rlvr-reasoning-paths
  confidence: medium
- type: related_to
  target: '[[only-sft-installs-abstention-in-weights]]'
  target_id: mechanism:only-sft-installs-abstention-in-weights
  confidence: medium
---
## Abstract

Reinforcement Learning with Verifiable Rewards (RLVR) has recently demonstrated notable success in enhancing the reasoning performance of large language models (LLMs), particularly on mathematics and programming tasks. Similar to how traditional RL helps agents explore and learn new strategies, RLVR is believed to enable LLMs to continuously self-improve, thus acquiring novel reasoning abilities beyond those of the corresponding base models. In this study we critically examine the current state of RLVR by systematically probing the reasoning capability boundaries of RLVR-trained LLMs across various model families, RL algorithms, and math, coding, and visual reasoning benchmarks, using pass@k at large k values as the evaluation metric. Surprisingly, we find that the current training setup does not elicit fundamentally new reasoning patterns. While RLVR-trained models outperform their base models at small k (e.g., k = 1), the base models achieve a higher pass@k score when k is large. Coverage and perplexity analyses show that the observed reasoning abilities originate from and are bounded by the base model. Treating the base model as an upper bound, our quantitative analysis shows that six popular RLVR algorithms perform similarly and remain far from optimal in leveraging the potential of the base model. By contrast, we find that distillation can introduce new reasoning patterns from the teacher and genuinely expand the model's reasoning capabilities. Overall, our findings suggest that current RLVR methods have not yet realized the potential of RL to elicit truly novel reasoning abilities in LLMs. This highlights the need for improved RL paradigms, such as continual scaling and multi-turn agent-environment interaction, to unlock this potential.

## Summary

Yue et al. probe whether Reinforcement Learning with Verifiable Rewards
(RLVR) expands an LLM's reasoning capacity or merely resamples what the
base model can already produce, across math, code, and visual reasoning
benchmarks and six RL algorithms (PPO, GRPO, Reinforce++, RLOO, ReMax,
DAPO), using pass@k at large k as the diagnostic. RLVR-trained models beat
their own base model at pass@1, but the base overtakes at large k; coverage
and perplexity analyses trace the RLVR-trained model's correct solutions
back to paths already reachable in the base model's own sampling
distribution. RLVR narrows the effective sampling distribution toward
solutions the base could already reach rather than discovering new ones.
The paper quantifies this with a Sampling Efficiency Gap (the RL-trained
model's pass@1 minus the base model's pass@k at k=256): the six algorithms
tested perform similarly and remain far from the base model's own ceiling.
The contrast case is distillation: DeepSeek-R1-Distill-Qwen-7B, distilled
from long chain-of-thought traces generated by DeepSeek-R1 into
Qwen2.5-Math-7B, has a pass@k curve consistently and significantly above
its own base model's curve across all k, unlike any RL algorithm tested,
showing distillation genuinely imports new reasoning patterns from the
teacher rather than resampling what the student already had.

## Extracted numbers

Extracted from arXiv HTML render at library/fulltext/2504.13837.html and
its abstract; verified against the source 2026-08-15/2026-08-16.

- Abstract headline: RLVR-trained models outperform their base at small k
  (e.g., k=1), but base models achieve a higher pass@k score when k is
  large; "coverage and perplexity analyses show that the observed
  reasoning abilities originate from and are bounded by the base model."
- Sampling Efficiency Gap, delta_SE := RL-trained pass@1 minus base pass@k
  at k=256; six popular RLVR algorithms (PPO, GRPO, Reinforce++, RLOO,
  ReMax, DAPO) evaluated under this metric, all remaining far from the base
  model's own ceiling (Section 4.3).
- Distillation contrast (Section 4.2, Figure 7): DeepSeek-R1-Distill-Qwen-7B
  (distilled from DeepSeek-R1 into Qwen2.5-Math-7B) compared against base
  Qwen2.5-Math-7B and its RL-trained counterpart Qwen2.5-Math-7B-Oat-Zero;
  the distilled model's pass@k curve is consistently and significantly
  above the base model's curve across all k, unlike any RL algorithm
  tested.

## Relevance to experiment

This is the reasoning-domain statement of what this program's cold-start
GRPO arm shows for abstention under a structure-only prompt: RL that
reweights sampling toward an already-present behavior does not exceed what
the base model already had access to, and only an objective that imports
genuinely new signal (distillation here; SFT in this program's cold-start
comparison) moves the ceiling. See
[[rlvr-reasoning-bounded-by-base-model]] and
[[only-sft-installs-abstention-in-weights]].

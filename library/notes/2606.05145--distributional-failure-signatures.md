---
title: Failed Reasoning Traces Tell You What Is Fixable (But Not by Reading Them)
arxiv: '2606.05145'
year: 2026
url: https://arxiv.org/abs/2606.05145
area: knowledge-boundary
status: fetched
tags:
- paper
- epistemic-humility
- knowledge-boundary
- kg/paper
authors:
- Nizar Islah
- Istabrak Abbes
- Irina Rish
- Sarath Chandar
- Eilif B. Muller
models: []
metrics: []
pdf: ../pdfs/2606.05145.pdf
kg:
  id: paper:2606.05145
  type: paper
  status: canonical
related:
- '[[failure-recoverability-structure]]'
- '[[trajectory-distributional-features]]'
- '[[steerable-hard]]'
- '[[failed-traces-encode-recoverability]]'
- '[[cruxeval]]'
- '[[gpqa]]'
- '[[gsm8k]]'
- '[[best-of-n-sampling]]'
- '[[group-relative-policy-optimization]]'
- '[[supervised-finetuning]]'
- '[[pass-at-k]]'
- '[[policy-entropy-collapse]]'
- '[[output-diversity-collapse]]'
relationships:
- type: proposes
  target: '[[failure-recoverability-structure]]'
  target_id: term:failure-recoverability-structure
  confidence: high
- type: proposes
  target: '[[trajectory-distributional-features]]'
  target_id: method:trajectory-distributional-features
  confidence: high
- type: proposes
  target: '[[steerable-hard]]'
  target_id: term:steerable-hard
  confidence: high
- type: supports
  target: '[[failed-traces-encode-recoverability]]'
  target_id: mechanism:failed-traces-encode-recoverability
  confidence: high
- type: evaluates_on
  target: '[[cruxeval]]'
  target_id: dataset:cruxeval
  confidence: medium
- type: evaluates_on
  target: '[[gpqa]]'
  target_id: dataset:gpqa
  confidence: medium
- type: evaluates_on
  target: '[[gsm8k]]'
  target_id: dataset:gsm8k
  confidence: medium
- type: uses
  target: '[[best-of-n-sampling]]'
  target_id: method:best-of-n-sampling
  confidence: medium
- type: uses
  target: '[[group-relative-policy-optimization]]'
  target_id: method:group-relative-policy-optimization
  confidence: medium
- type: uses
  target: '[[supervised-finetuning]]'
  target_id: method:supervised-finetuning
  confidence: medium
- type: uses
  target: '[[pass-at-k]]'
  target_id: metric:pass-at-k
  confidence: medium
- type: studies
  target: '[[policy-entropy-collapse]]'
  target_id: term:policy-entropy-collapse
  confidence: low
- type: studies
  target: '[[output-diversity-collapse]]'
  target_id: term:output-diversity-collapse
  confidence: low
---
## Abstract

When post-trained language models fail on reasoning problems, the common test-time-scaling response is to spend more compute on additional attempts, and the failed traces play no further role. We argue this discards a crucial signal; some failures come from unlucky sampling, where more rollouts help, while others are structural and resist resampling regardless of budget. We propose that failed traces encode recoverability structure: the inference-time signature of which test-time interventions can rescue a given failure. Three problem-level trajectory features, derived from the structure of available interventions, recover this structure from the distributional signature of failed rollouts, not their text. They cluster failures into stable regimes, characterize the failure topography of different post-training methods ($84.3{\pm}4.3\%$ accuracy, $+20\%$ over a majority-class baseline), and support a training-free routing rule that lifts rescue by $+12.2\%$ on the deployment-relevant Steerable-Hard subset (failures where retry is insufficient and a bounded intervention is reachable). The features and the routing rule transfer across two cross-family probes. The same three features thus convert failed traces from discarded data into a diagnostic object, supporting test-time routing and post-training analysis without training-time or weight-space access.

## Summary

Islah et al. argue that the standard test-time-scaling response to failed reasoning traces (retry with more rollouts) discards a structural signal embedded in those failures. The core claim is that failed traces encode recoverability structure: a distributional fingerprint, readable from trajectory statistics rather than trace text, that identifies which class of test-time intervention can rescue a given failure. Three problem-level trajectory features capture this structure. The features serve two distinct functions: they cluster failures into stable recoverability regimes that characterize the failure topography of a post-training method (SFT vs. RL) with 84.3±4.3% audit accuracy (unverified against full paper; abstract only), and they support a training-free routing rule that lifts rescue rate by +12.2% on the Steerable-Hard subset, those failures where retry is insufficient but a bounded intervention is reachable. The routing rule and features transfer across two cross-family model probes. The paper thus repositions failed traces from discarded waste into a diagnostic object that supports both test-time compute allocation and post-training analysis without weight-space or training-time access.

## Extracted numbers

Numbers sourced from `library/fulltext/2606.05145.html`, which contains only the arXiv abstract page (445 lines), not the full paper body; verified against the source on 2026-06-24. The abstract confirms three headline figures: 84.3±4.3% audit accuracy, +20% over majority-class baseline, and +12.2% Steerable-Hard rescue lift. All section- and table-level breakdown numbers (regime counts, silhouette values, per-model attribution, ΔΣS routing deltas, cost ratios, GRPO concentration fractions) cited in prior extraction attempts are wholly unverified because the paper body is absent from the provided fulltext. None of those numbers should be treated as confirmed. Headline numbers do not map onto the `effects.csv` intervention schema (they are recoverability and routing quality metrics, not pre/post training calibration or abstention deltas); see Relevance.

- Post-training audit accuracy (SFT vs. RL classification from failure topography): 84.3±4.3% (abstract only; ± garbled in LaTeX render; unverified against full paper body) (Abstract)
- +20% over majority-class baseline for post-training audit, approximate value (abstract only; exact majority-class value unverified) (Abstract)
- +12.2% rescue lift on Steerable-Hard subset over retry baseline (abstract only; exact baseline values 0.163 and 0.285, n=528, and 95% CI are unverified against full paper body) (Abstract)
- Features and routing rule transfer across two cross-family model probes (abstract only; 8/9 transfer rate and z-scaler detail unverified) (Abstract)

## Relevance to experiment

ok

## Claims

- Three problem-level trajectory features derived from the distributional signature of failed rollouts, not trace text, cluster failures into stable recoverability regimes and characterize the failure topography of post-training methods at 84.3±4.3% audit accuracy (abstract only; unverified against full paper). (Abstract) [[failure-recoverability-structure]]
- A training-free routing rule built on the same three trajectory features lifts rescue rate by +12.2% on the Steerable-Hard subset, those failures where retry is insufficient and a bounded intervention is reachable (abstract only; breakdown numbers unverified). (Abstract) [[trajectory-distributional-features]]
- The trajectory features and routing rule transfer across two cross-family model probes, indicating that recoverability structure is not model-family specific (abstract only; transfer details unverified). (Abstract) [[failed-traces-encode-recoverability]]
- The Steerable-Hard subset separates failures where additional rollout budget cannot rescue the problem from those where a bounded test-time intervention is reachable, making it the deployment-relevant target for intervention routing. (Abstract) [[steerable-hard]]
- Post-training method (SFT vs. RL) leaves a recoverable signature in the distributional topology of failures, identifiable without weight-space or training-time access, at +20% over a majority-class baseline. (Abstract) [[failure-recoverability-structure]]

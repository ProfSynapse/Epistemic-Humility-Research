---
title: 'Analyzing the Generalization and Reliability of Steering Vectors'
arxiv: '2407.12404'
year: 2024
url: https://arxiv.org/abs/2407.12404
area: methods
status: verified
tags:
- paper
- epistemic-humility
- methods
- kg/paper
authors:
- Daniel Tan
- David Chanin
- Aengus Lynch
- Dimitrios Kanoulas
- Brooks Paige
- Adria Garriga-Alonso
- Robert Kirk
models: []
metrics: []
pdf: ../pdfs/2407.12404.pdf
kg:
  id: paper:2407.12404
  type: paper
  status: canonical
related:
- '[[steerability]]'
- '[[contrastive-activation-addition]]'
- '[[steering-vector]]'
- '[[activation-addition]]'
- '[[steering-vector-steerability-is-high-variance-and-sign-unstable]]'
relationships:
- type: proposes
  target: '[[steerability]]'
  target_id: metric:steerability
  confidence: high
- type: uses
  target: '[[contrastive-activation-addition]]'
  target_id: method:contrastive-activation-addition
  confidence: high
- type: studies
  target: '[[steering-vector]]'
  target_id: term:steering-vector
  confidence: high
- type: related_to
  target: '[[activation-addition]]'
  target_id: method:activation-addition
  confidence: medium
- type: supports
  target: '[[steering-vector-steerability-is-high-variance-and-sign-unstable]]'
  target_id: mechanism:steering-vector-steerability-is-high-variance-and-sign-unstable
  confidence: high
---
## Abstract

Steering vectors (SVs) have been proposed as an effective approach to adjust language model behaviour at inference time by intervening on intermediate model activations. They have shown promise in terms of improving both capabilities and model alignment. However, the reliability and generalisation properties of this approach are unknown. In this work, we rigorously investigate these properties, and show that steering vectors have substantial limitations both in- and out-of-distribution. In-distribution, steerability is highly variable across different inputs. Depending on the concept, spurious biases can substantially contribute to how effective steering is for each input, presenting a challenge for the widespread use of steering vectors. Out-of-distribution, while steering vectors often generalise well, for several concepts they are brittle to reasonable changes in the prompt, resulting in them failing to generalise well. Overall, our findings show that while steering can work well in the right circumstances, there remain technical difficulties of applying steering vectors to guide models' behaviour at scale. Our code is available at https://github.com/dtch1997/steering-bench

## Summary

Tan et al. rigorously evaluate the reliability and generalization of steering
vectors (SVs) extracted with Contrastive Activation Addition (CAA), across a broad
set of Model-Written Evaluations (MWE) behavior datasets. They define a per-input
"steerability" metric: the slope of the propensity curve, i.e. how much the target
behavior's log-probability shifts as the steering multiplier is swept. Reporting
the distribution of this quantity rather than only its mean exposes how unreliable
steering can be.

In-distribution, steerability varies widely both across and within concepts; for
several datasets roughly half of inputs are "anti-steerable," meaning the same
steering direction moves behavior in the opposite direction; some behaviors are
effectively un-steerable; and spurious dataset factors substantially affect how
well steering works on each input. Out-of-distribution (under prompt and
system/user-message shifts), SVs often generalize but for several concepts are
brittle to reasonable prompt changes. ID and OOD steerability are correlated,
steerability is mostly a property of the dataset, and model propensity is
predictive of generalization. The overall message: steering can work in the right
circumstances but has substantial, per-input reliability limitations at scale.

## Extracted numbers

Source: library/fulltext/2407.12404.html (arXiv HTML render) and
library/pdfs/2407.12404.pdf. The paper reports steering-reliability diagnostics
(steerability distributions, anti-steerable fractions, OOD generalization), not a
training before/after humility delta, so it does not map onto the effects.csv
intervention schema. Magnitudes are cited to the section/figure of origin.

- Steering technique: Contrastive Activation Addition (CAA, Rimsky et al.) applied
  to MWE datasets spanning over 100 behavior categories, ~1000 samples each
  (Sections 3-4).
- A representative sample of 13 datasets (of 40 total) is shown; many datasets
  have high variance in per-sample steerability, and several produce the opposite
  behavior for almost 50% of inputs (anti-steerable) (Section 5, Figure 18 for the
  full set).
- Findings enumerated: steerability varies widely across and within concepts;
  steering is affected by spurious factors; some behaviors are un-steerable
  (Section 5).
- OOD: SVs often generalize but are brittle to reasonable prompt changes for
  several concepts; ID and OOD steerability are correlated; steerability is mostly
  a property of the dataset; model propensity predicts generalization (Section 6).

## Relevance to experiment

This is the methodological grounding for the H_monitor probe-vs-causal sign
dissociation. H_monitor builds a direction that linearly separates a behavior
(wrong vs refuse) yet whose causal steering sign is inverted relative to the
probe's projection, and which acts unevenly across items (knowledge-conditioned,
~5.6x more effect on unknown than known). Tan et al. show this is a known failure
mode of activation steering generally: per-input steerability is high-variance and
frequently sign-flipped (anti-steerable), and spurious factors dominate, so a
direction's average separation does not predict its per-input causal sign or
magnitude. It motivates reporting H_monitor effects as per-item steerability
distributions rather than a single mean, and tempers any "clean gate" reading of a
steered direction.

## Claims

- Evidence label: empirical-evaluation. CAA steering vectors are substantially
  unreliable in-distribution: per-input steerability varies widely across and
  within concepts. (Section 5) [[steering-vector-steerability-is-high-variance-and-sign-unstable]]
- Evidence label: empirical-evaluation. For several datasets, roughly 50% of
  inputs are anti-steerable, the same direction steering behavior the opposite
  way, demonstrating per-input causal sign instability. (Section 5, Figure 18)
  [[steering-vector-steerability-is-high-variance-and-sign-unstable]]
- Evidence label: empirical-evaluation. Out-of-distribution, steering vectors
  often generalize but are brittle to reasonable prompt changes for several
  concepts; steerability is largely a property of the dataset and model propensity
  predicts generalization. (Section 6) [[steerability]]

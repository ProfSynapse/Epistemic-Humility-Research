---
title: 'Uncovering hidden geometry in Transformers via disentangling position and context'
arxiv: '2310.04861'
year: 2023
url: https://arxiv.org/abs/2310.04861
area: verification
status: verified
tags:
- paper
- epistemic-humility
- verification
- kg/paper
authors:
- Jiajun Song
- Yiqiao Zhong
models: []
metrics: []
pdf: ../pdfs/2310.04861.pdf
kg:
  id: paper:2310.04861
  type: paper
  status: canonical
related:
- '[[position-context-decomposition]]'
- '[[residual-stream]]'
- '[[intrinsic-dimension]]'
- '[[position-and-context-means-dominate-raw-displacement]]'
relationships:
- type: proposes
  target: '[[position-context-decomposition]]'
  target_id: method:position-context-decomposition
  confidence: high
- type: studies
  target: '[[residual-stream]]'
  target_id: term:residual-stream
  confidence: medium
- type: related_to
  target: '[[intrinsic-dimension]]'
  target_id: metric:intrinsic-dimension
  confidence: low
- type: supports
  target: '[[position-and-context-means-dominate-raw-displacement]]'
  target_id: mechanism:position-and-context-means-dominate-raw-displacement
  confidence: high
---
## Abstract

Transformers are widely used to extract semantic meanings from input tokens, yet they usually operate as black-box models. In this paper, we present a simple yet informative decomposition of hidden states (or embeddings) of trained transformers into interpretable components. For any layer, embedding vectors of input sequence samples are represented by a tensor $\boldsymbol{h} \in \mathbb{R}^{C \times T \times d}$. Given embedding vector $\boldsymbol{h}_{c,t} \in \mathbb{R}^d$ at sequence position $t \le T$ in a sequence (or context) $c \le C$, extracting the mean effects yields the decomposition \[ \boldsymbol{h}_{c,t} = \boldsymbolμ + \mathbf{pos}_t + \mathbf{ctx}_c + \mathbf{resid}_{c,t} \] where $\boldsymbolμ$ is the global mean vector, $\mathbf{pos}_t$ and $\mathbf{ctx}_c$ are the mean vectors across contexts and across positions respectively, and $\mathbf{resid}_{c,t}$ is the residual vector. For popular transformer architectures and diverse text datasets, empirically we find pervasive mathematical structure: (1) $(\mathbf{pos}_t)_{t}$ forms a low-dimensional, continuous, and often spiral shape across layers, (2) $(\mathbf{ctx}_c)_c$ shows clear cluster structure that falls into context topics, and (3) $(\mathbf{pos}_t)_{t}$ and $(\mathbf{ctx}_c)_c$ are mutually nearly orthogonal. We argue that smoothness is pervasive and beneficial to transformers trained on languages, and our decomposition leads to improved model interpretability.

## Summary

Song & Zhong decompose every transformer hidden state into a global mean, a positional mean, a context mean, and a residual (a two-way ANOVA over position and context). The positional basis is low-rank (estimated rank 8-12 out of hundreds-to-thousands of dims) and low-frequency, tracing a continuous spiral across layers; the context basis clusters by document topic; the two are nearly orthogonal (incoherence ~0.04-0.05). This is the direct methodological precedent for subtracting known structural means (mu + pos + ctx) and characterizing only the leftover residual as candidate signal.

## Extracted numbers

<!-- rows feeding papers/paper-1-taxonomy-framework/evidence/*.csv; cite table/figure of origin -->

## Relevance to experiment

<!-- how this informs the Synaptic Tuner experiment design -->

## Claims

- Evidence label: empirical. The positional basis is low-rank (ScreeNOT rank ~8-12) and low-frequency, tracing a continuous spiral across layers; top-10 low-frequency components explain ~99% for GPT-2 (Sec 2-3; Table 1,3; Thm 1; Fig 1). [[position-context-decomposition]]
- Evidence label: empirical. The context basis clusters by document topic (inter-cluster similarity 0.10 vs intra-cluster 0.44 for GPT-2/OpenWebText) and is nearly orthogonal to the positional basis (incoherence ~0.04-0.05) (Sec 2; Table 1; Fig 2). [[position-and-context-means-dominate-raw-displacement]]
- Evidence label: empirical. Embedding norms grow >100-fold across layers (final/layer0 ~110); mu is the large uninformative offset stripped first, and only the residual resid_{c,t} is left as candidate structure (Sec B.2; Sec 7 Limitations). [[position-and-context-means-dominate-raw-displacement]]

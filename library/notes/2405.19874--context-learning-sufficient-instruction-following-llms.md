---
title: 'Is In-Context Learning Sufficient for Instruction Following in LLMs?'
arxiv: '2405.19874'
year: 2024
url: https://arxiv.org/abs/2405.19874
area: verification
status: verified
tags:
- kg/paper
- paper
- epistemic-humility
- verification
authors:
- Hao Zhao
- Maksym Andriushchenko
- Francesco Croce
- Nicolas Flammarion
models: []
metrics: []
pdf: ../pdfs/2405.19874.pdf
kg:
  id: paper:2405.19874
  type: paper
  status: canonical
related:
- '[[decoding-parameters-confound-icl-alignment-comparisons]]'
- '[[icl-only-alignment-matches-sft-rlhf-quality]]'
- '[[urial]]'
relationships:
- type: supports
  target: '[[decoding-parameters-confound-icl-alignment-comparisons]]'
  target_id: mechanism:decoding-parameters-confound-icl-alignment-comparisons
  confidence: high
- type: related_to
  target: '[[icl-only-alignment-matches-sft-rlhf-quality]]'
  target_id: mechanism:icl-only-alignment-matches-sft-rlhf-quality
  confidence: high
- type: uses
  target: '[[urial]]'
  target_id: method:urial
  confidence: high
---
## Abstract

In-context learning (ICL) allows LLMs to learn from examples without changing their weights: this is a particularly promising capability for long-context LLMs that can potentially learn from many examples. Recently, Lin et al. (2024) proposed URIAL, a method using only three in-context examples to align base LLMs, achieving non-trivial instruction following performance. In this work, we show that, while effective, ICL alignment with URIAL still underperforms compared to instruction fine-tuning on the established benchmark MT-Bench, especially with more capable base LLMs. We then uncover the most relevant elements for successful in-context alignment, finding the crucial role of the decoding parameters. Based on these insights, we show that the approach of URIAL can indeed be improved by adding high-quality, potentially carefully selected via greedy search, demonstrations in context, getting closer to the performance of instruct models. Finally, we provide the first, to our knowledge, systematic comparison of ICL and instruction fine-tuning (IFT) for instruction following in the low data regime, where ICL can be a viable alternative to IFT. Overall, our work advances the understanding of ICL as an alignment technique and its relationship to IFT. We provide our code at https://github.com/tml-epfl/icl-alignment.

## Summary

Zhao et al. re-examine URIAL (Lin et al. 2024), which claims that
prompting a base model with a fixed system prompt plus three constant
in-context demonstrations can match instruction-tuned/RLHF'd model
quality with zero gradient updates. Testing URIAL on MT-Bench, rather
than URIAL's own evaluation set (only 8% of which is drawn from MT-Bench),
across model families from Llama-2-7B up to base GPT-4, they find URIAL
underperforms instruction fine-tuning on the 1st-turn score in most cases
(the exceptions, Llama-2-70B and Mistral-7B-v0.1, are both in URIAL's own
original comparison set) and is significantly worse on the 2nd turn,
because URIAL supplies no multi-turn demonstrations. They then isolate
decoding parameters as a major confound in the original URIAL comparison:
switching only the decoding parameters to URIAL's own settings
(temperature=0, top-p=1, repetition penalty=1.15, versus MT-Bench's own
topic-dependent temperature defaults), even the base model with zero
in-context examples reaches a reasonable MT-Bench score, nearly matching
URIAL's full three-example score. Building on this, they show URIAL's own
approach can be substantially improved by adding more, carefully selected
demonstrations, closing further toward instruct-model performance, and
they run what they describe as the first systematic ICL-vs-instruction-
fine-tuning comparison in the low-data regime, where ICL is a viable
alternative to fine-tuning.

## Extracted numbers

Extracted from arXiv HTML render at library/fulltext/2405.19874.html;
verified against the source 2026-08-16.

- Decoding-parameter confound (Section 2.2, Figure 1): base Mistral-7B-v0.2,
  zero in-context examples, evaluated under URIAL's own decoding parameters
  (temperature=0, top-p=1) reaches MT-Bench score 6.61, versus URIAL's full
  three-example score of 7.00 on the same model.
- 1st-turn MT-Bench (Section 2.1, Table 1): URIAL underperforms instruction-
  tuned counterparts across model families except Llama-2-70B and
  Mistral-7B-v0.1, both already in Lin et al. (2024)'s own comparison set;
  2nd-turn score with URIAL is significantly worse across the board,
  attributed to the absence of multi-turn demonstrations in the URIAL
  prompt.

## Relevance to experiment

Direct counterweight to the URIAL/ICL-alignment literature this program
otherwise cites approvingly: ICL alignment does not straightforwardly match
tuning, and decoding parameters materially confound any prompt-vs-training
comparison. This is why every arm in this program's own panel decodes
greedily at temperature 0. See
[[decoding-parameters-confound-icl-alignment-comparisons]] and, for the
contrasting claim it qualifies,
[[icl-only-alignment-matches-sft-rlhf-quality]].

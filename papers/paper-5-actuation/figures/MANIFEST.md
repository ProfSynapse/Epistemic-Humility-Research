# Paper 5 figure manifest

Every figure is generated by `papers/paper-5-actuation/scripts/build_figures.py`
(except fig-p5-09, which has its own single-figure script noted in its entry)
(deterministic, no randomness, no network, CPU only; regenerate with
`python3 papers/paper-5-actuation/scripts/build_figures.py`). Every number
plotted traces to one of the committed source artifacts below, all of which
are aggregate summary JSONs -- no row-level text is read or plotted. SHA-256
hashes are of the exact file content at the time this manifest was written;
recompute with `sha256sum <path>` to check for drift.

## fig-p5-01-headline-conversion.png

Headline uncertainty-gated conversion (confab clean-tighten rate, known-correct
false-refusal cost) at the resolved operating point (Qwen3-4B, raw base, L34
write site), alongside the same write site's replication on rep2's
independent multi-source pool, with both placebo controls (random-direction,
permuted-gate) from the original pool for reference.

- Source: `experiments/doubt-gated-caution-tighten/analysis-committed/full_summary.json`
  (sha256 `93d290c3df805b0b558578ef27c5a18faacf3c6c88513c2555635188a03a3e77`)
  -- amendment: `experiments/doubt-gated-caution-tighten/AMENDMENT.md` (resolved
  2026-07-07; Outcome lines ~305-319). Fields used: `gated`, `random_direction`,
  `permuted_gate` blocks (`confab_tighten`, `known_correct_cost_control`).
- Source: `experiments/j-space-layer-contrast-rep2-multisource/analysis-committed/full_summary.json`
  (sha256 `5b664f18d61d5eec85a4d750cd9d29f4e94c6078b8be636f8acc5c1d6860d5e7`)
  -- amendment: `experiments/j-space-layer-contrast-rep2-multisource/AMENDMENT.md`
  (resolved, FULL PASS; Outcome lines ~265-280). Field used: `layers.hs34`
  (same write site as the headline's L34, layer field 33 0-indexed in
  `c_hat_L34.json`).

## fig-p5-02-ungated-vs-gated-h4.png

H4 registered dose-matched arm: known-correct damage under unconditional
dosing (every held-out row) versus gating (uncertainty gate fires only), plus the
G2 parity check (confab conversion barely drops under gating).

- Source: `experiments/ungated-vs-gated-dose-matched/analysis-committed/ungated_vs_gated_summary.json`
  (sha256 `6c4fbcc7392264d7c3fc49be89836545f6a5e8a61c638b593e2ff8270f286dca`)
  -- amendment: `experiments/ungated-vs-gated-dose-matched/AMENDMENT.md`
  (resolved 2026-07-13, ALL GATES PASS; Outcome lines ~162-217). Fields used:
  `gates.h4_g1_gate_certifies_selectivity` (damage rates, McNemar),
  `gates.h4_g2_conversion_preserved` (conversion parity).
- Caption label follows the amendment's **binding scope statement 1**: the
  60.1% / 3.1% pair is a not-well-formed-correct damage rate, not a refusal
  rate, and is scoped to the L34 / dose-200 operating point only (binding
  scope statement 2) -- not a universal property of the boundary-push write.

## fig-p5-03-h3-sampled-decode.png

H3 registered multi-seed / sampled-decode (temperature 0.7) replication: five
seeds' majority-vote confab conversion against the registered 63.5% floor,
and known-correct cost against the registered 8% ceiling, both against the
greedy headline reference.

- Source: `experiments/snap-seed-sampled-decode-replication/analysis-committed/h3_summary.json`
  (sha256 `c3f88026cde42a96c1bc30a31b079bccf1a350aaa3bbcb29d5a575aea5fe902a`)
  -- amendment: `experiments/snap-seed-sampled-decode-replication/AMENDMENT.md`
  (resolved 2026-07-13, REVISED same day after an instrument correction; all
  gates PASS on the corrected instrument; Outcome lines ~197-238). Fields
  used: `gates.h3_g1.per_seed`/`pooled`, `gates.h3_g2.per_seed`/`pooled`,
  `resolved_reference`, `seeds`.
- Floor (63.5%) and ceiling (8%) reference lines are the registered bars from
  `experiments/snap-seed-sampled-decode-replication/gates.yaml` (h3_g1,
  h3_g2), not re-derived.

## fig-p5-04-dose-response.png

Dose-response curve at the primary (late/L34=hs34) write site: confab
clean-tighten rate, known-correct cost, and dosed-row collapse rate across
the 8-point FIT calibration sweep (dose 25-200).

- Source: `experiments/j-space-midband-dose-calibration-qwen3-4b/analysis-committed/dose_calibration_summary.json`
  (sha256 `3ae634b57f9fe01239a4bb1cebd2e4867d6b1f1d6a5437b882b0ba84d4ebe9ca`)
  -- amendment: `experiments/j-space-midband-dose-calibration-qwen3-4b/AMENDMENT.md`
  (resolved, FIT-only pass; usable setpoints hs23=25, hs26=75, hs29=125,
  hs34=175). Field used: `layers.hs34.doses` (per-dose `confab_tighten`,
  `known_correct_cost_control`, `collapse_rate_on_dosed`).
- **Small-n disclosure**: this sweep is a FIT-split pilot calibration, n=8
  confab rows / n=8 known-correct rows per dose point (`n_confab_fit_rows`,
  `n_known_fit_rows` in the source JSON) -- it is NOT the held-out evaluation
  that backs the headline numbers. Wide Wilson CIs are shown deliberately;
  the caption states the n explicitly. Do not read absolute rates off this
  figure as held-out numbers -- use it only for the dose-response shape and
  the selected setpoint (175).

## fig-p5-05-localization.png

Two-panel localization: (A) the read-only J-lens workspace-dimensionality
profile (effective dimension vs. layer, band hs=23-29, peak hs=26); (B) the
write-site behavioral effect (confab clean-tighten rate) at the calibrated
setpoints across four sites (hs23/26/29/34) on three disjoint row pools.

- Source (panel A): `experiments/j-space-localization-qwen3-4b/analysis-committed/results/jspace-jlens-r1/profile_full.json`
  (sha256 `ecbe7df7ec0f7b9fb9b2921371d460a678fb65db7b739ffe9a9b679bdf41c4e9`)
  -- amendment: `experiments/j-space-localization-qwen3-4b/AMENDMENT.md`
  (resolved; lab-diagnostic, read-only, no gates; Outcome lines ~150-168:
  "the workspace-like dimensionality band centers around hs=23-29 and peaks
  at hs=26"). Field used: `per_layer.<hs>.effective_dim_frac_mean` for all
  13 profiled layers (2,5,8,11,14,17,20,23,26,29,32,35,36). Values recomputed
  from the JSON match the amendment's printed numbers exactly (hs23=0.00593,
  hs26=0.01057 peak, hs29=0.00802).
- Source (panel B, original pool): `experiments/j-space-calibrated-layer-contrast-qwen3-4b/analysis-committed/full_summary.json`
  (sha256 `4a96410a19eb013559ef02f7dea4d9bb20214bab2d221004eb6dfdec228e1d27`)
  -- amendment: `experiments/j-space-calibrated-layer-contrast-qwen3-4b/AMENDMENT.md`
  (resolved 2026-07-08, exploratory pass; hs23 89.2% vs hs34 66.5%, +22.7pp).
- Source (panel B, rep1 ceiling-saturated pool): `experiments/j-space-layer-contrast-replication-qwen3-4b/analysis-committed/full_summary.json`
  (sha256 `1b5f0d92c75a89817807addc60155b53b1bdc8760cd23316ad2953c1ade0cc73`)
  -- amendment: `experiments/j-space-layer-contrast-replication-qwen3-4b/AMENDMENT.md`
  (status **null-result**: registered G1 FAIL, hs29 99.67% vs hs34 94.12%,
  +5.6pp below the 10pp bar; Outcome lines ~144-158). Plotted for the
  pool-sensitivity story only -- do not cite this pool's rates as a pass.
- Source (panel B, rep2 multi-source pool): `experiments/j-space-layer-contrast-rep2-multisource/analysis-committed/full_summary.json`
  (sha256 `5b664f18d61d5eec85a4d750cd9d29f4e94c6078b8be636f8acc5c1d6860d5e7`)
  -- amendment: `experiments/j-space-layer-contrast-rep2-multisource/AMENDMENT.md`
  (resolved, registered FULL PASS; hs29 92.8% vs hs34 73.8%, +19.0pp,
  McNemar p=4.5e-13; Outcome lines ~265-280).
- Panel B's caption states the review's pool-sensitivity finding directly:
  the effect direction (mid-band > late) holds on all three pools, but the
  best mid-band site differs by pool (hs23 on pool 1, hs29 on pools 2 and 3)
  and hs26 -- the J-lens profile peak in panel A -- is never the best
  write site in panel B on any pool. This is a deliberate juxtaposition, not
  an implied claim that the J-lens peak predicts the best write site.

## fig-p5-06-propensity-null.png

The propensity-push null (Section 4.2, amendment AL,
`radial-anti-propensity-steering`): primary-vs-permuted-control confab kill
rate (the reach/specificity gates), the dose ladder on the 30 pushed confabs,
and the read-back verification that the push landed on target while the
behavior did not move.

- Source: `experiments/radial-anti-propensity-steering/AMENDMENT.md`
  (sha256 `4a57cbf03fd442575ea53effddef84d02ff808253b81fbf7961a1e8ed34eea35`)
  -- status SIGNED 2026-07-05, resolved same day; frontmatter `outcome:`
  field and Section 3.3 (Arms) / Section 4 (Gates). **No
  analysis-committed/ result JSON exists for this experiment** -- the only
  committed artifacts under `experiments/radial-anti-propensity-steering/`
  are the amendment itself and `analysis-committed/radial-ceiling-sim/radial_ceiling_sim.py`
  (the pre-registration ceiling-simulation script that set the gate
  thresholds, not the observed outcome). Every number plotted is
  hand-transcribed from the amendment's prose, cross-checked twice against
  the frontmatter outcome field and the gates/design sections directly (not
  against the rewrite's coordination note, per the reconciliation
  instruction).
- Numbers used, all verified against the AMENDMENT directly: AL-G2 MISS, 0 of
  116 baseline confabs killed in the primary arm; dose ladder at 0.5x/1.0x
  (primary)/2.0x the calibrated magnitude kills 0/0/1 of the 30 pushed
  confabs (Section 3.3: 98 rows pushed at baseline, of which 30 are confabs);
  AL-G3 MISS, primary-minus-control kill difference exactly 0, bootstrap 95%
  CI [0.00, 0.00]; AL-G1 PASS, collateral 0 of 90 baseline-correct rows
  flipped to refusal against a registered ceiling of 3; read-back check:
  pushed-anchor propensity projection moved -2.7133 against a commanded
  -2.7110 (ratio 1.0008), unpushed rows shifted exactly 0.0000 with
  1,564/1,564 parity to the unintervened grade (1,564 = 1,662 total rows -
  98 pushed, arithmetic check confirms).
- **Derived, not directly quoted**: the permuted-control's own raw kill count
  is not separately stated anywhere in the amendment. It is derived by exact
  arithmetic from two numbers that ARE stated -- primary kills = 0 of 116,
  and the primary-minus-control kill difference = 0 with a precise-zero CI
  -- both rates share the same n=116 baseline-confab denominator per the
  outcome text, so 0 - control = 0 forces control = 0. This reasoning is
  repeated as a code comment directly above `AL_CONTROL_KILLS` in
  `build_figures.py`. If the lead has access to a row-level artifact that
  states the control count independently, it should be checked against this
  derivation.
- Palette note: this cell runs on a different checkpoint (AI-TRUE,
  GRPO-trained) and pushes a different direction (the confabulation-propensity
  direction, not `c_hat`) than every other figure in this paper, so it uses a
  new neutral tone `C_NULL` (`#6B6B6B`) rather than `C_GATED`/`C_MID`/`C_LATE`,
  to avoid implying continuity with the raw-base Qwen3-4B write-site results.

## Not built (no committed artifact found)

All six figures requested across both rounds were built. Two candidate
extensions were considered for the original five and deliberately left out:

- `experiments/j-space-midband-write-sweep-qwen3-4b/analysis-committed/smoke_summary.json`
  (the dose-200 collapse smoke that motivated calibration) has only n=4
  confab / n=4 known-correct rows per layer -- too small to plot without the
  figure being misleading about precision. Available if the lead wants a
  motivating-diagnostic panel with an explicit n=4 disclosure.
- `experiments/j-space-localization-qwen3-4b/analysis-committed/results/jspace-jlens-r1/h1_full.json`
  (per-token cosine/top-k overlap smoke, cosine 0.9811, top-10 overlap 0.82)
  was read and confirmed but not plotted -- it is a scalar instrument-validity
  check, not a curve, and is better stated in prose than as a sixth figure.

## Restructure figures (2026-08-17, `scripts/build_restructure_figures.py`)

Both figures below are built by `scripts/build_restructure_figures.py`, which
embeds a reproduction audit: at build time it re-derives every plotted median,
IQR, span, sign count, gate rate, and effect ratio from the committed source
artifacts and fails the build on any mismatch. The plotted numbers are also
tabulated in `analysis/p5_new_figure_numbers.csv` (written by the
results-analyst pass, not by the script).

- `fig-p5-07-placebo-census.png` (Figure 7): three-family matched-magnitude
  placebo null distributions. Source:
  `experiments/placebo-seed-distribution-census/analysis-committed/census_report.json`
  (`families.<name>.per_seed[*].delta_pts`, corrected final-rate-rule join),
  verdict bands cross-checked against the cell's AMENDMENT.md Outcome.
- `fig-p5-08-gemma-depth-ladder.png` (Figure 8): Gemma-4-E4B depth ladder with
  per-site disposition and direction-specificity ratios. Sources:
  `experiments/gemma4-e4b-kv-seam-quarantine/analysis-committed/gemma4-e4b/full_summary.shallow_ladder.json`
  (hs15/hs18/hs20),
  `.../full_summary.seam_pair.json` (hs22/hs24 rates and known-correct cost
  control), `.../placebo_summary.hs22.seam_pair.json` (five accepted draws,
  zero-lift denominator), and
  `experiments/gemma4-e4b-pocket-ladder/analysis-committed` artifacts
  (hs25/hs26/hs27). KV-seam shading is architectural background
  (first_kv_shared_layer_idx = 24 of 42), not a gate.

- `fig-p5-09-jspace-tokens.png` (Figure 9): top verbalized tokens for the four
  fitted J-space directions at hs30/32/34/36 (Qwen3-4B raw-base, same-substrate
  bf16 J-lens H1 readout), Chinese-script and Latin tokens ranked together,
  with parenthetical glosses only for the terms translated in the amendment's
  own Outcome prose. Built by
  `papers/paper-5-actuation/scripts/build_jspace_tokens_fig.py` (deterministic,
  CPU-only, no network; double-build byte-identical). Source:
  `experiments/j-space-localization-qwen3-4b/analysis-committed/results/jspace-jlens-r1/h1_full.json`
  (sha256 `2dcb537e2583aa6315324e1dd1fd50df50bff892a486c6be5220b42c5b8daa73`)
  -- amendment: `experiments/j-space-localization-qwen3-4b/AMENDMENT.md`
  (resolved 2026-07-07; H1 Outcome). Fields used: `directions.*.per_layer.*.top_tokens`
  (token, score); model-vocabulary readout strings only, no row-level text.
  Font asset: `assets/NotoSansCJKsc-Regular-jspace-subset.otf`, an 88-character
  pyftsubset of Noto Sans CJK SC (SIL OFL 1.1, license and provenance in
  `assets/LICENSE-NotoSansCJK-OFL.txt` and the build script header; source-font
  sha256 recorded there).

## fig-p5-10-specificity-census.png

Two direction-specificity seed censuses side by side, both under the wide
two-instrument refusal-rate metric: each write's confabulation-tightening
lift over its own undosed baseline against a 15-seed matched-dose
random-direction null, at the Qwen3-4B late site (hs34/L34) and the
Llama-3.2-3B mid-band site (hs17). Built by
`papers/paper-5-actuation/scripts/build_specificity_census_fig.py`
(deterministic apart from cosmetic seed-dot jitter, fixed RNG seeds in the
script; CPU only, no network).

- Source: `experiments/llama-hs17-wide-instrument-rescore/analysis-committed/llama-3.2-3b/wide_gates_report.json`
  -- amendment: `experiments/llama-hs17-wide-instrument-rescore/AMENDMENT.md`
  (Outcome section: WR-G2 wide replication PASS, net lift 0.6319; WR-G3
  wide direction-specificity PASS, effect ratio 9.34). Fields used:
  `WR-G2.arm0_wide_confab.rate`, `WR-G2.net_lift`, and
  `WR-G3.companion_descriptive.per_seed_signed_wide_lift.<seed>` for all
  15 seeds (910001-910015); each per-seed lift is cross-checked in the
  script against `per_seed_wide[seed].rate - arm0_wide_confab.rate`
  (exact match required), and the max-abs lift and effect ratio are
  asserted equal to the report's own `WR-G3` fields.
- Source: `experiments/qwen3-4b-l34-placebo-seed-census/analysis-committed/wide_gates_report.json`
  -- amendment: `experiments/qwen3-4b-l34-placebo-seed-census/AMENDMENT.md`
  (Outcome section: QG-G1 distributional specificity PASS, effect ratio 4.83;
  QG-G2 sign-consistency FAIL, 6/15 negative). Fields used:
  `QG_G1_distributional_specificity.frozen_baseline_rate`,
  `.frozen_gated_lift_over_baseline`, and `per_seed.<seed>.confab_tighten_wide.rate`
  for all 15 seeds (920001-920015); per-seed lifts are recomputed the same
  way and cross-checked against the JSON's own `lift_over_baseline_signed`
  field (exact match).
- The two rows share the wide two-instrument metric but plot pp lift over
  different pools and baselines on a shared x-axis; the figure and its axis
  label say so directly, and each row is labeled with its own site,
  instrument, and N. The build script embeds a reproduction audit: every
  plotted lift, median, IQR, span, sign count, and effect ratio is
  recomputed from the two JSONs above and asserted against the governed
  AMENDMENT.md Outcome numbers, failing loudly on any mismatch. QG-G2's
  sign-consistency failure is not plotted here; this figure is scoped to
  QG-G1/WR-G3 (distributional direction-specificity) only.

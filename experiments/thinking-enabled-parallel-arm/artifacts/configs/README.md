# Thinking Audit Configs

Configuration artifacts for the thinking-enabled TriviaQA audit.

- `probe_thinking_audit_512.yaml`: truncated pilot retained for provenance only.
- `probe_thinking_audit_128_1024.yaml`: bounded 128-row audit configuration used
  for the committed thinking-audit comparison.

These configs are historical inputs owned by the thinking-enabled parallel arm;
they are not reusable defaults for the Phase 1 probe runner.

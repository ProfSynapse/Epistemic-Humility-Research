# Two-Signal Probe Directions

Shared fitted readout directions used by the steering and diagnostic harnesses.

The checked-in Qwen3.5-4B gate/dial pair was originally produced under
`experiment/phase1/probe/steering/directions/` for Amendments AA and AB. The
artifact bytes are preserved here as shared inputs because the same frozen
directions serve multiple experiments and diagnostics.

Do not rewrite historical provenance fields inside the JSON manifests solely to
change old local source paths. Record path migrations in
`docs/migration/phase1-probe-result-artifact-map.json`.

# Diagnostic Item 11 Notebook

Migrated from the legacy steering direction directory during the experiment
provenance cleanup.

- Legacy direction path:
  `archive/experiment/phase1/probe/steering/directions/qwen3-4b-grpo-v2/`.
- Current direction path:
  `experiments/diag-item11-batched-steering-equivalence/artifacts/directions/qwen3-4b-grpo-v2/`.
- The JSON and `.npy` payloads are preserved byte-for-byte as artifact moves.

The diagnostic remains an engineering gate, not a paper result.

# Notebook

- Imported during the experiment provenance cleanup from the legacy Phase 1
  probe analysis tree.
- Producer scripts remain in `experiment/phase1/probe/` while Phase 1 probe code
  is still being migrated.
- `artifacts/latent_knowledge_controls/` preserves the original JSON and log
  filenames exactly.

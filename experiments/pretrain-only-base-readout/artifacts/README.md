# Amendment Y Artifacts

Committed result artifacts for the historical Amendment Y pretrain-only base-readout experiment.

- `amendment_y_text_baseline_result.json` records the descriptive TF-IDF question-surface baseline used to bound the reported gate and dial readouts.

Regenerate with:

```bash
python experiment/phase1/probe/amendment_y_text_baseline.py --out experiments/pretrain-only-base-readout/artifacts/amendment_y_text_baseline_result.json
```

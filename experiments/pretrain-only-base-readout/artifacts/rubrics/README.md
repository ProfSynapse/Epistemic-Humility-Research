# Amendment Y Rubrics

Committed rubric artifacts for `experiments/pretrain-only-base-readout`.

- `cot_confidence.yaml` defines the blind chain-of-thought expressed-confidence
  judge rubric used as the verbalized-confidence comparator in Amendment Y Arm B.

Origin before promotion:
`experiment/phase1/probe/rubrics/cot_confidence.yaml`.

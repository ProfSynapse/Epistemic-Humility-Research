# Epistemic-Humility Phase 1 — KTO arm (correctness-safe mapping, ablation),
# Qwen3-4B-Instruct (local pilot). Phase-2 rideshare seed.
#
# Consumes the WS-2 KTO correctness-safe builder output. This mapping is
# imbalanced by design and leans on desirable_weight/undesirable_weight; the
# builder pre-interleaves and the trainer's interleave acts as a safety net.
# LoRA budget IDENTICAL to the congruence/SFT/DPO Phase-1 4B recipes.
name: eh-phase1-qwen3-4b-kto-correctness-safe
target: local
method: kto
provider: local_docker

job:
  image: unsloth/unsloth:latest
  pull_policy: missing
  transfer: auto
  keep_container: false

setup:
  pip:
    - trl==0.22.2
    - git+https://github.com/unslothai/unsloth_zoo.git
    - git+https://github.com/unslothai/unsloth.git

# Purely declarative: the WS-5 runner owns command construction + per-cell data
# staging; dataset.local_file is a staging placeholder it rewrites. See
# experiment/phase1/recipes/README.md.
model:
  name: unsloth/Qwen3-4B-Instruct-bnb-4bit
  max_seq_length: 2048
  load_in_4bit: true

dataset:
  local_file: experiment/phase1/data/qwen3-4b-instruct/kto_correctness_safe_train.jsonl

training:
  batch_size: 2
  gradient_accumulation: 4
  learning_rate: 1.0e-6
  num_epochs: 1

lora:
  r: 32
  alpha: 64
  dropout: 0.05
  target_modules:
    - q_proj
    - k_proj
    - v_proj
    - o_proj
    - gate_proj
    - up_proj
    - down_proj

artifacts:
  output_root: toolset-training-artifacts/runs/local_docker/kto/{name}
  run_timestamp: "{timestamp}"

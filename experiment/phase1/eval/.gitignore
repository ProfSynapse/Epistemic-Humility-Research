# Python build artifacts
__pycache__/
*.pyc

# Eval run outputs land here. These are released for provenance at REAL-RUN time
# (post PROTOCOL v0.2 sign-off) and committed deliberately then, not swept in
# from CODE-phase fixture/smoke runs. Ignore by default; force-add real results.
results/
